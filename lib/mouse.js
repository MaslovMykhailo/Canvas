'use strict';

class Mouse {
  constructor() {
    this.x = 0;
    this.y = 0;
  }
}

/*

const mouse = new Mouse();

const changeMouse = (e) => {
  mouse.x = e.pageX;
  mouse.y = e.pageY;
}

document.canvas.addEventListener('mousemove', changeMouse);

*/